services:
  sg_transport_mcp:
    build: .
    ports:
      - "8001:8001"
    env_file: .env
    environment:
      - MCP_TRANSPORT=streamable-http
      - FASTMCP_HOST=0.0.0.0
      - FASTMCP_PORT=8001
    restart: unless-stopped
